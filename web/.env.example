# Base URL of the Flask API (server-side only, never exposed to the browser)
API_BASE=http://127.0.0.1:5700

# Base URL of the legacy Flask-served pages (used for the Apply flow until it
# is migrated into this app)
NEXT_PUBLIC_LEGACY_BASE=http://localhost:5700
